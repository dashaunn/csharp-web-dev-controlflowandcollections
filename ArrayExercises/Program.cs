using System;

namespace ArrayExercises
{
    class Program
    {
        static void Main(string[] args)
        {
            // todo 1: Create and initialize an array with the following values in a single line
            Console.WriteLine("Hello Arrays!");
            int[] numberArray = new int[6] { 1, 1, 2, 3, 5, 8 };
            foreach (int num in numberArray)
            {
                // todo 2: Loop through the array and print out each value.
                //Console.WriteLine(num);

                // todo 3:Modify the loop to only print the odd numbers from the array.
                
            }
        }
    }
}
